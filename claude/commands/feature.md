Your goal is to work on the following task:

$ARGUMENTS

First, feel free to create a complete implementation, run unit tests and integration tests, confirm it works.

Once done, commit all the changes to a new draft branch and split the changes into separate, small, easy to review commits/pull requests in new feature branch. Each pull request/commit must leave the system in the working state. If some part of the functionality is impossible to split up without breaking tests, don't split it.

The goal is to keep the changes easy to review for every commit.
